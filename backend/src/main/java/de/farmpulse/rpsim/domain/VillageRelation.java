package de.farmpulse.rpsim.domain;

/** Onboarding building block "Verhältnis zum Dorf" (UNKNOWN = neutral default, no trust offset). */
public enum VillageRelation {
    UNKNOWN,
    STRAINED,
    CONNECTED
}
