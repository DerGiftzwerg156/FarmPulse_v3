# Open questions to the project owner

Log of decisions that are not answered by `docs/concept/*`. Format: one row per question.

| Date | AP | Question | Proposed default | Status |
| --- | --- | --- | --- | --- |
| 2026-09-25 | AP-0.1 | License: the work-package plan proposes MIT unless the owner decides otherwise. | MIT (committed as `LICENSE`, copyright "Keno (DerGiftzwerg156)"). Please confirm or name another license. | open – default applied |
| 2026-09-25 | AP-3.1 / AP-4.5 | How many game days form one "game month" (salaries, installments, `EMPLOYEE_EFFECT`)? The FS25 period field is an open technical point. | `rpsim.time.game-days-per-month = 1` (FS25 default "days per period"); change in config if you play with more days per period. | open – default applied |
| 2026-09-25 | AP-4.2 | The concept leaves the normalisation of the five credit metrics open ("mit Sättigung"). Without a pro-forma view an absurd request (e.g. 50 Mio. on a 0.5 Mio. farm) still reached the counter-offer band because equity and missing cash-flow history dominate. | `equityRatio` is computed pro forma after financing: (assets − debt) / (assets + requested). Saturation = linear up to a configurable "full" ratio, then capped at 100. With no cash-flow history `debtServiceCoverage` uses the configurable placeholder `debt-service-coverage-no-history: 50`. | open – default applied |
| 2026-09-25 | AP-4.4 | Selling own fields: the concept only says "same round logic, roles swapped". Which limit does the NPC buyer use, and how are auction rounds shown? | Mirror formula: NPC buyer accepts up to `basePrice · (1 + stubbornnessDiscount) · (1 + trustAdjustment)`; a demand within 1/0.9 of that limit yields a counter offer at the limit. The NPC's first offer (`askingPrice · random(0.85, 1.0)`) is capped at that limit so absurd asking prices cannot create absurd offers. Auctions: a player bid above the highest NPC limit wins immediately; otherwise the auctioneer reports the leading bid `min(npcMaxBid, playerBid + 1 % basePrice)`; after round 3 or at the deadline the highest NPC wins and becomes owner. Unowned map fields are assigned to dynamic NPCs with `npc-owned-share = 0.4` when the ownership table is first built. | open – default applied |
| 2026-09-25 | AP-4.5 | `effectMultiplier = clamp(score/100, 0.5, 1.2)` can only reach a bonus (> 1.0) if the need categories may exceed 100 – with categories in 0..100 the maximum score is 100 (multiplier 1.0). Should employees be able to produce a bonus? | Formula implemented literally; each category is bounded by the new config `satisfaction.category-max` (default 100 → malus only). Setting it to 120 enables the 1.2 bonus without code changes. | open – default applied |
| 2026-09-25 | AP-4.5 | Salary delay: one negative `payFairness` event per overdue episode, or one per missed month? | One event per overdue episode (the salary stays due and is paid as soon as liquidity allows). | open – default applied |
| 2026-09-25 | AP-5.4 | Which Gemini model is currently free (Google AI Studio free tier)? The official docs (ai.google.dev) are not reachable from the build environment, so the model name could not be verified at implementation time. | Default `gemini-2.5-flash` (free-tier model at the last verifiable state); purely configurable (`rpsim.ai.gemini.model` or the settings page). Please verify against https://ai.google.dev/gemini-api/docs/pricing before first use. | open – default applied |
| 2026-09-25 | AP-5.3 | Default Anthropic model for narration. | `claude-opus-5` (current default recommendation) with server-side refusal fallbacks; for lower cost a smaller model (e.g. `claude-haiku-4-5`) can be set in the settings page – the model name is configuration only. | open – default applied |
